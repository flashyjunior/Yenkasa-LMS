// DebugSeedController removed. This file kept as a placeholder to avoid file-delete issues in the environment.
// Temporary debug endpoints were used during development to trigger the seeder on-demand.
// They have been intentionally removed. Use DatabaseSeeder.Seed(context) via startup or run direct DB queries for verification.

namespace LMS.Controllers
{
    // No-op placeholder
    internal static class DebugSeedControllerPlaceholder { }
}
